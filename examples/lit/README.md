# Reglow + Lit example

A runnable “Northstar Launch Room” settings flow using Reglow Custom Elements in a Lit template.

```sh
pnpm --filter @reglow/example-lit dev
```

The example demonstrates `.options`, `.value`, and `.checked` property expressions, native
`@input` events, and the Reglow `@rg-press` custom event.
