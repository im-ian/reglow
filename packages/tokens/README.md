# @reglow/tokens

Framework-neutral Reglow design tokens and theme CSS.

```ts
import '@reglow/tokens/css';
```

Token values are also available as typed JavaScript exports from `@reglow/tokens`.
Named JavaScript imports are side-effect-free and tree-shakable; theme CSS is included only through
the explicit `@reglow/tokens/css` entry.
